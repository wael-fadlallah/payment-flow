import React from 'react';

export default function page() {
  /**
   * Noting to see here,
   * we are just redircing to the employees page because we don't have a main dashboard page
   */
  return <></>;
}
